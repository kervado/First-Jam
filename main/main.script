local store = require("main.settings_store")

function init(self)
	-- Global, shared across scripts (shared_state = 1 in game.project).
	-- SETTINGS.volume is the 0..1 master volume the settings slider reflects.
	SETTINGS = store.load()
	store.apply(SETTINGS)
end
